# Local Jekyll Usage guide

Uses docker container so don't forget to have Docker running

## Run local `jekyll` server

> `docker-compose up -d`

Yes just that simple!

## Run local `jekyll` commands

> `docker-compose run jekyll /bin/bash`

